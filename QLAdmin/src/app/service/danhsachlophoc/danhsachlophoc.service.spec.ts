/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { DanhsachlophocService } from './danhsachlophoc.service';

describe('Service: Danhsachlophoc', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [DanhsachlophocService]
    });
  });

  it('should ...', inject([DanhsachlophocService], (service: DanhsachlophocService) => {
    expect(service).toBeTruthy();
  }));
});
